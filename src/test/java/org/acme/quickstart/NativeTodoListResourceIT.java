package org.acme.quickstart;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeTodoListResourceIT extends TodoListResourceTest {

    // Execute the same tests but in native mode.
}